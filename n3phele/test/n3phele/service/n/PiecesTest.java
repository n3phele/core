package n3phele.service.n;

import n3phele.service.nShell.ParseException;
import n3phele.service.nShell.UnexpectedTypeException;

import org.junit.Test;

public class PiecesTest {
	
	@Test
	public void testCommandLine() throws IllegalArgumentException, ParseException, UnexpectedTypeException {
/*		
		context.putValue("not_barcoded", false);
		context.putValue("disable_primer_check", false);
		context.putValue("variable_len_barcodes", false);
		context.putValue("verbose",  false);
		
		context.putObjectValue("command", eval( 
				"check_id_map.py -m map.txt -o mapping_output \"+" +
				"$$not_barcoded?\"-b \":\"\" " +
				"$$disable_primer_check?\"-p \":\"\" " +
				"$$variable_len_barcodes?\"-B \":\"\" " +
				"$$verbose?\"-v \":\"\"", context));
		
		Assert.assertEquals("check_id_map.py -m map.txt -o mapping_output ", context.getValue("command"));
	*/		
		
	}


}
